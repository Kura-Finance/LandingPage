/** Brand mark served from /public (stable URL, long-cache). */
export const brandIconUrl = '/icon.webp'

/** Brand mark with opaque background — use on dark surfaces (footer, dark header). */
export const brandIconOnDarkUrl = '/icon_background.webp'
